package imagetools.model;

/**
 * This class represents an enumeration of color change operations the client can perform on an
 * image (e.g. applying sepia or gray scale).
 */
public enum ColorOp {
  Sepia, GrayScale
}
