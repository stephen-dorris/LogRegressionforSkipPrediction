package imagetools.model;

/**
 * This class represents an enumeration of filter operations that the client can do on an image (e.g
 * Blur an image, sharpen an image).
 */
public enum FilterOp {
  Blur, Sharpen
}
